import styled from 'styled-components';

export const Container = styled.div`
  margin-top: 3rem;
  margin-inline: 4rem;
`;

export const ThemeHeader = styled.h3`
  font-size: 1.8rem;
  margin-bottom: 1rem;
`;

export const ColorContainer = styled.div`
  display: flex;
  gap: 1.5rem;
`;
